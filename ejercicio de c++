#include <iostream>
#include <string>

using namespace std;

int main() {
    string frase;

    cout << "Introduce una frase: ";
    getline(cin, frase);

    reverse(frase.begin(), frase.end());

    cout << frase << endl;

    return 0;
}
